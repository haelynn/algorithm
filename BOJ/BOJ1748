#include <iostream>
#include <string.h>

using namespace std;

int main(){
    std::ios::sync_with_stdio(false);


    int n;
    cin >> n;

    int ans = 0;
    for(int i=1; i<=n; i *= 10){
        ans += n - i + 1;
    }
    cout << ans << "\n";
}
